{
  "homepage": "https://1carew1.github.io/tax-bi-weekly",
  "name": "tax_bi_weekly",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "clone": "^1.0.4",
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0"
  }
}
